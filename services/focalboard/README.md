# Focalboard (Reserved)

This directory is currently reserved and does not contain an active service module.

Use this path only when a real `focalboard` module is introduced with the standard layout:

- `focalboard.nix`
- `docker-compose.yml` or generated compose renderer
- service-specific documentation and options
